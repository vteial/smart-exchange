function bnsService($log, $q, wydNotifyService, sessionService, dataServcie, $http) {
    var service = {};

    service.init = function() {

    };

    return service;
}
appServices.factory('bnsService', bnsService);